<nav class="navbar">
    <div class="logo">
        <a href="/">☕ CoffeeBlog</a>
    </div>

    <div class="nav-links">
        <a href="/" class="<%= activePage === 'home' ? 'active' : '' %>">Home</a>
        <% if(isAuthenticated) {%>
        <a href="/blogs" class="<%= activePage === 'notes' ? 'active' : '' %>">Blogs</a>
        <a href="/myblogs" class="<%= activePage === 'mynotes' ? 'active' : '' %>">My Blogs</a>
        <% }%>
    </div>

    <div class="nav-links">
        <% if(isAuthenticated){ %>
            <form action="/logout" method="post">
            <input type="hidden" name="_csrf" value="<%= _csrf %>">

                <button type="submit">Log Out</button>
            </form>

          <% }else{ %>  
        <a href="/login">Log In</a>
        <a href="/signup">Sign Up</a>
          <% } %>
        </div>
    </div>
</nav>